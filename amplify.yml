version: 1
platform: linux # Top level
applications: # Top level
  - appRoot: .
    backend: # Indented under applications list item
      phases:
        build:
          commands:
            - echo "Using Amplify container build via 'image' config below."
    image: # Indented under applications list item (same level as backend)
      dockerfilePath: Dockerfile
      repository: study-app-image # Ensure this matches your ECR repo name
# --- Minimal Frontend Section (Required by schema) ---
frontend: # <--- NO INDENTATION HERE (Top level)
  phases: # Indented under frontend
    build: # Indented under phases
      commands: []
  artifacts: # Indented under frontend
    files: []
  cache: # Indented under frontend
    paths: []