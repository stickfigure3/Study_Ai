@bp.route('/health')
def health_check():
    # Optionally add a quick DB check
    # try:
    #     db.session.execute(db.text('SELECT 1'))
    #     return jsonify(status='ok'), 200
    # except Exception as e:
    #     current_app.logger.error(f"Health check DB error: {e}")
    #     return jsonify(status='error', reason='db_connection'), 503
    return jsonify(status='ok'), 200 # Simple check first